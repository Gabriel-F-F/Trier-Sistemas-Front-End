import { MultiplicadorPipe } from './multiplicador.pipe';

describe('MultiplicadorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicadorPipe();
    expect(pipe).toBeTruthy();
  });
});
